---
name: inbox-clear
description: "Dismiss all unread events without reading them. Use when the user wants to clear their inbox without spending context on the messages."
---

# /inbox-clear — Dismiss Unread Events

Mark all unread events as read without fetching or displaying them. This saves context by not loading message content.

```bash
handler ack
```

After running, confirm briefly: "Inbox cleared."
